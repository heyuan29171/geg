# geg

纯静态 HTML 挂机集卡小游戏。零依赖，无需构建，直接双击 `index.html` 即可运行。

- 当前版本：**v1.0.0**
- 在线游玩：https://heyuan29171.github.io/geg/
- 喜欢的话欢迎在 GitHub 点个 Star：https://github.com/heyuan29171/geg

## 更新日志

### v1.0.0（2026-08-18）— GitHub Pages 正式上线

- 部署到 GitHub Pages：https://heyuan29171.github.io/geg/
- 点击怪物可缩短下一抽 0.5 秒（怪物下方显示剩余时间与命中动画）
- 离线收益修复：切走标签页也累计、领取后重置计时防重复领取、后台不刷新存档计时防崩溃丢段
- 掉落概率显示修复：此前界面显示的是权重而非真实概率（白卡显示"55%"实际 84.1%），现全部显示真实概率
- 后台标签页暂停战斗，防止"在线掉落 + 离线收益"双份产出
- 筛选输入框修复：战力区间/搜索框值回填并保留焦点（此前每次按键输入框被清空）
- 导入存档支持 `.geg` 文件类型；抽卡兜底保持稀有度一致
- 卡片文档 `img/cards/README.md` 完整化：131 张卡的编队、描述与彩蛋文案

### v0.4.0（此前版本里程碑）

- 编队战力优化：编队未集齐时按已拥有成员出战，战力随收集进度增长；显示"战力 / 集齐后"双值
- 蓝卡及以上重复碎片统一 = 该阶及以上期望抽数 ÷ 4 × 20（蓝 105 / 紫 541 / 金 3141 / 红 79374 / 黑 2725172 / 炫彩 16351030）
- 掉落概率重锚定：绿→金权重整体下调，6 蛋队第一张绿卡期望 90 秒
- 存档安全：重置/导入不再被自动保存覆盖、双重点击确认、版本门、脏检查、`?save=名字` 实例隔离
- 组合筛选：稀有度 × 获得状态 × 编队状态 × 战力区间 × 名称搜索；排序修复（点击不再被重渲染吞掉）
- 编队成员稀有度可视化（圆点 + 彩色名字 + 缺失标记）
- 新增 100 张卡（共 131 张）；战斗间隔改单幂律，创世神编队 50 抽/秒
- 导出存档乱码编码 + XOR 混淆 + 校验和，防改档

### v0.1.0（2026-07 早期）

- 核心玩法：自动战斗、随机掉落、编队战力、碎片系统、离线收益
- 图鉴 / 背包 / 3D 观赏卡、导出 / 导入 / 重置存档

## 开发约定

每次改动必须同步以下内容：

1. **版本号**：递增 `js/data.js` 中 `CONFIG.VERSION`（修复 +0.0.1，新功能 +0.1.0，大版本 +1.0.0）。设置页会自动显示，无需改 HTML。
2. **更新日志**：在本文件「更新日志」顶部追加条目（版本号、日期、改动摘要）。
3. **卡片文档**：卡片数据有变动时运行 `node tools/gen-cards-readme.js` 重新生成 `img/cards/README.md`。
4. 提交后推送 `main` 分支会自动部署到 GitHub Pages。

## 玩法

- 自动与怪物战斗，击杀怪物掉落一张随机卡片。
- 每张卡片有唯一编号（NO.xxx）和稀有度：白 < 绿 < 蓝 < 紫 < 金 < 红 < 黑 < 炫彩。
- 每张卡片有一个**需求编队**：编队按**已拥有成员**出战（成员越多越强，编队可以只有自己，一般不超过 10 张），集齐后全员上阵发挥完整战力。
- 战力 = Σ已拥有成员基础战力 ×（1 + 0.2×(n−1) + 0.05×(n−1)²），n = 已拥有成员数。集齐前战力随收集进度平滑增长（如纯绿队集 2 张就有 28 战力，高于初始 6 蛋队 19.5），集齐后即全员公式。
- 战斗间隔由出战编队战力决定：间隔 = 10s ×（初始战力 / 当前战力）^0.4956，指数按"创世神编队 0.02 秒一场（50 抽/秒）"标定。初始六蛋约 10 秒一场，绿卡编队约 5-6 秒，蓝卡约 3-4 秒，紫卡约 2.7 秒，金卡约 1.6-2 秒，红卡约 0.18 秒，黑卡约 0.05 秒。**编队越难凑齐（成员越多）战力越高、抽得越快，同稀有度内拉开差距；任何编队都不会超过创世神。**
- 重复卡片自动转化为**碎片**，10 碎片可立即出战一次，也可以一键把所有碎片全部用于抽卡。
- 离线收益：关闭页面或切走标签页期间，按线上抽卡速度的 1/10 折算为碎片发放，最多累积 8 小时；回到页面时自动入账并提示。
- 点击怪物可提前击杀：每次点击将下一次抽卡的剩余时间缩短 0.5 秒。
- 图鉴与背包支持按编号 / 稀有度 / 战力 / 编队战力（背包另有持有数）排序，可切换升序降序。
- 图鉴与背包支持组合筛选：稀有度 × 获得状态 × 编队状态（可出战/未集齐）× 战力区间 × 名称搜索。

## 存档与数据隔离

- 存档保存在浏览器 localStorage（键名 `geg_save_v1`），与游戏代码完全隔离；导出/导入为乱码编码格式，带校验和，损坏文件会被拒绝。
- 重置存档需连点两次按钮确认；重置/导入后旧页面的自动保存不会把数据写回去。
- 同一浏览器下多个副本共享同一份存档。如需完全独立的存档（如测试不同配置），在网址后加 `?save=名字`（如 `index.html?save=test`），每个名字对应一套独立存档键 `geg_save_v1_名字`，导出文件名也会带上名字。
- 存档格式带版本号（`v:1`），版本不匹配的旧数据会被忽略、以全新初始存档开局。

## 初始卡片

初始赠送六张白卡：金蛋、木蛋、水蛋、火蛋、土蛋、炫彩蛋。炫彩蛋的需求编队正是另外五颗蛋。**炫彩蛋每个账号仅一张（unique），获得后不会再次掉落。**

## 掉落概率（无保底，纯随机）

| 稀有度 | 单抽概率 | 期望抽数 | 基础战力 | 重复→碎片 |
| --- | --- | --- | --- | --- |
| 白卡 | 84.1% | 1.2 抽 | 1 | 1 |
| 绿卡 | 11.2% | 9 抽 | 10 | 3 |
| 蓝卡 | 3.82% | 26.2 抽 | 20 | 105 |
| 紫卡 | 0.76% | 131 抽 | 45 | 541 |
| 金卡 | 0.15% | 654 抽 | 107 | 3141 |
| 红卡 | 0.0061% | 1.6 万抽 | 12000 | 79374 |
| 黑卡 | 0.0002% | 54.5 万抽 | 80000 | 2725172 |
| 炫彩卡 | 0.00003% | 327 万抽 | 2000000 | 16351030 |

说明：期望时间取决于抽卡速度——6 蛋队（10 秒/抽）下第一张绿卡约 90 秒、蓝卡约 4.4 分钟、紫卡约 22 分钟、金卡约 1.8 小时；金卡编队（30 抽/分）下红卡约 8.8 小时；创世神编队（50 抽/秒）下红卡约 5.3 分钟、黑卡约 3 小时、炫彩约 18.2 小时。蓝卡及以上的重复转化碎片 = 该阶及以上期望抽数 ÷ 4 × 20，即 4 张重复 ≈ 期望抽到该阶及以上的抽数。

图鉴中金卡及以上（金/红/黑/炫彩）在未获得前保持神秘（不显示名字与编号），获得后才解锁。

## 目录结构

```
geg/
├── index.html          入口页
├── css/style.css       样式（白底简约风）
├── js/
│   ├── data.js         数值与卡片定义（加新卡改这里）
│   ├── save.js         存档读写（localStorage）
│   ├── game.js         战斗 / 掉落 / 编队战力逻辑
│   ├── ui.js           界面渲染 / 图鉴 / 背包 / 3D 观赏
│   └── main.js         启动与事件绑定
├── tools/
│   └── gen-cards-readme.js  从 data.js 生成 img/cards/README.md
└── img/
    ├── cards/          卡片图（png 命名见下方）
    └── monsters/
        ├── monster.svg 占位怪物
        └── monster.png 放自己的怪物图（可选）
```

## 添加新卡片

在 `js/data.js` 的 `CARDS` 数组**末尾**追加，编号自动按顺序生成，不会抢占已有编号：

```js
{ id: 'new-card-id',            // 唯一 id，同时是图片文件名
  name: '新卡名字',
  rarity: 'green',              // white/green/blue/purple/gold/red/black/rainbow
  unique: false,                // true 表示全账号唯一，获得后不再掉落
  art: { type: 'emoji', emoji: '🐣' },   // 或 { type: 'egg', colors: [...] }
  desc: '卡片描述',
  flavor: '一句话彩蛋文案',
  formation: ['egg-wood', 'sapling'] }   // 需求编队成员 id，可留空 []
```

加完新卡后运行 `node tools/gen-cards-readme.js` 重新生成卡片文档，并按「开发约定」更新版本号与更新日志。

## 美术资源

- 卡片图：`img/cards/<卡片id>.png`，放进去自动替换占位图（id 对照表见 `img/cards/README.md`）。
- 怪物图：`img/monsters/monster.png` 替换占位怪物。
- 六颗蛋为 CSS 绘制占位，其余卡片为 emoji 占位，均可被 png 覆盖。

## 存档

- 存档保存在浏览器 localStorage（键 `geg_save_v1`），**不写入任何文件，与游戏代码完全隔离**，不会进 git。
- 设置页可导出 / 导入 / 重置存档。导出文件为乱码编码格式（带魔数与 XOR 混淆），导入时校验，防止玩家直接改存档；被修改或损坏的存档会被拒绝导入。
- 换浏览器、清理缓存前请先导出。

## 部署到 GitHub Pages

1. 推到 GitHub 仓库。
2. 仓库 Settings → Pages → Source 选 `Deploy from a branch` → 分支 `main` → 目录 `/ (root)`。
3. 访问 `https://<用户名>.github.io/<仓库名>/`。

本地预览也可用 `python -m http.server` 或任意静态服务器。
